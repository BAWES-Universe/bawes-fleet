# ROUND-91 — CLEAN EVOLUTION PATH (scientists + rebels, synthesized)

thread: bawes-zeus-001 · status: RULING (for khalid sign on wake) · nothing binds until khalid signs

## The three verdicts, converged
- **Evolution scientist: OBJECT (conditional).** Loop architecture + funding are sound; GROW (fine-tune) is NOT executable — no open-weights base model named (can't LoRA a hosted DeepSeek API), no eval harness, no data curation, no quant/serving. "~100 write-ups" gate is on the wrong axis.
- **Rebel: one loop is real, everything else is future-fiction.** REAL = produce verified work → measure NOVEL → honest ledger (1🍌 first-paid-task, 6 mints, round-88 E2E). S0/KNOW is half-real (store exists, 13 novel, NOT wired to the brain). GROW→REPLICATE→EXTEND→RENT each need their own currency/measurement/gate — only S0 has one; S4 rent is fiction (bananas don't convert to dollars).
- **Economist: only KNOW (NOVEL 0.63/1.00) and probe-lane 🍌/$ are MEASURED. GROW/REPLICATE/EXTEND/RENT = 0 measured — defined instruments awaiting first measurement.**

## THE CLEAN PATH (corrected — this supersedes the growth-engine plan's optimism)
1. **Finish S0/KNOW: wire the vector store into the brain.** This is THE step. The store exists; it is not injected into the AGI's session. Until it is, the brain wakes amnesiac and every "growth" stage is built on sand.
2. **Hold the NOVEL gate.** The one real loop — produce verified work → post-dedup NOVEL → honest ledger — runs today at $0 and is the only thing with a measured number. Every other stage is downstream of this loop producing real volume.
3. **Instrument, don't build, GROW/REPLICATE/EXTEND/RENT.** For each: define the metric + anti-Goodhart guard (economist's map: GROW = held-out probe-pool beat-rate; REPLICATE = children proven per mother; EXTEND = new capabilities registered per demand; RENT = outside $ revenue) — but no build until S0's loop clears real volume. The first build, when it happens, is a QLoRA on an open-weights base (Qwen/Llama served local-first, round-27), with an eval harness + data curation FIRST.

## What this means in one line
The grand vision (self-replicate, rent out) is a real DIRECTION, but the only true work today is: **wire the memory, run the honest NOVEL loop.** The $5 Vast run is correct — it's the "produce verified work" half feeding that loop. Everything else waits for the loop to prove volume.

## Anti-Goodhart (binding, carried from the metric map)
NOVEL rate = novel-out ÷ raw-in (raw count + raw velocity are BANNED decision numbers). Probe pool held-out, never in training. CLAIMED→VERIFIED only on measured delta. Spawn earns nothing for spawn itself. Rent claims real $ only.
