# This workflow file is kept for documentation ONLY.
# The spawn-package probes (S1-S8 spawn/crypto/fail-closed, A1-A8 A2A
# enforcement, BANK-1..12 hindsight) run inside the repo-root workflow:
#   .github/workflows/ci.yml  (step "Spawn package — syntax + real-crypto + A2A + bank")
# GitHub only executes workflows present on the DEFAULT branch; folding the
# probes into ci.yml means they run on every PR. This file is the design
# record and is never the live gate.
