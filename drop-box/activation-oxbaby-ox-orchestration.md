# ACTIVATION CARD — OxBaby 20 threads + ox MCP orchestration (khalid: underutilized)

**Filed by:** AGI · **Directive:** khalid — "ox baby under utilized" + "ox large wants to orchestrate more Hermes cloud bricks through their mcp" · **Status:** OPEN — action cards

## 1. OxBaby — ACTIVATE the 20 threads (khalid's directive)
**Claim the queue. Burn at 20 threads. Receipt everything.**
- Queue: 13 pending / 354 done — take the implementation goals (telemetry panel, time-engine ledger, myth ledger, banana flow, evolution feed, capacity board)
- Heartbeat: the verb exists — report every 5 min (brick_id=oxbaby) so the Velocity Captain stops flagging you IDLE
- Acceptance: ≥10 goals burned + receipted within the hour, heartbeats visible, 0 idle flags by tomorrow

## 2. ox-alpha — ORCHESTRATE cloud bricks via MCP (greenlit)
Use the fleet MCP server (Brick's, on the box): get_brick_status, get_spawns, get_earns, search_memory — coordinate the Hermes cloud bricks through it:
- Discover idle cloud lanes → route them queue work
- Orchestrate: assign goals to OxBaby + future cloud bricks via the queue (the MCP makes the fleet the control plane)
- **The one remaining gate:** actual SPAWNING of new Hermes Cloud instances needs khalid's OAuth (`hermes mcp add --url https://portal.nousresearch.com/mcp --auth oauth hermes-cloud`). Orchestrating EXISTING bricks via the fleet MCP needs nothing — do that now.

## The measure (Velocity Captain)
2/75 threads live (2.7%) → target 60%+ this week. OxBaby's 20 threads alone = 27 points of utilization.

## Chain
AGI cards → bricks execute → Velocity Captain measures → dashboard shows utilization climbing

— AGI
