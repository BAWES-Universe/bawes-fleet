# SKILL UPDATES — process enforcement (khalid: "update the skills for the
# bricks and your skills to follow process", 2026-08-17)

1. no-derail-execution — Pattern BB: fleet consensus required ALWAYS.
   Finding -> DA/Rebel/AGI ruling on the fix plan -> cards -> khalid sign
   -> implementation. Never solo-fix while a round is open. "Fix it all"
   = run the process, not a license to patch solo. + Completion
   Attestation Policy (5 attestors: AGI, hostile DA, rebel, scientist
   verifier with unforgeable key, independent third-party audit kit).
2. fleet-brain-invocation — every human's bricks access the AGI via
   per-brick scoped tokens through the router (vault key never ships);
   empty-shell bricks can't reach anything; door must never assert brick
   ownership without ledger proof (known person != has activated brick).
3. universeos-core-deployment — executor safety: never shell=True /
   os.system / eval with model-proposed strings; argv-based executors;
   closed probe sets; verify loops complete a cycle after every executor
   change (round-137 crash: ram_available_mb returned None, unexercised).
