# ROUND-148 — OWN-THE-BRAIN RULING (DA OBJECT + rebel future-fiction)

thread: bawes-zeus-001 · nothing binds until khalid signs

## Verdict: the 3-stage roadmap is future-fiction. Collapse to ONE MVP.

### DA (OBJECT) — 5 findings
1. **Data pipeline = future-fiction.** 43–120 docs vs the fleet's OWN floor (~100k labeled interactions / ~100 verified write-ups). 2–3 orders of magnitude short. "Extracted Discord signal" re-triggers round-93 OBJECT (consent V-5 breach — hashed IDs = pseudonymization, not consent). "dedup/format" = one word each, no tooling.
2. **Benchmark = gamed-by-structure.** The verifier reasons through DeepSeek — so "Qwen ≥ DeepSeek" is a 51/49 tie that passes. No N, no margin, no blind grading.
3. **Phase-in thresholds = made-up.** No measured call volume anywhere; break-even is pure arithmetic, not a fleet measurement.
4. **DeepSeek stays = the only sound part.**
5. **Cost/ROI = unmeasured** (fleet has no call-volume number).

### Rebel (future-fiction)
- Zero of the 3 stages is proven. 5 Qwen runs tonight (~$18 budgeted), all "attempt/retry" — vllm driver, corpus path bug, stuck hosts. The fleet hasn't gotten Qwen to produce ONE verified answer yet.

## RULING
- **Stage 2 (fine-tune) + Stage 3 (serve/phase-in): KILLED** until data floor (100k/100-writeup) + real consent + measured volume >150/hr.
- **Stage 1 (audit/benchmark): approve ONLY as a capped burst ($3–5)** with a non-DeepSeek grader + real margin (not a 51/49 tie).
- **DeepSeek stays default — binding, unchanged.**

## The ONE honest MVP (prove THIS first)
> Qwen 27B **loads reliably → ingests the live vector store → produces ONE non-earner-verified finding that matches/beats DeepSeek on the same prompt** — at $0 new spend (the ~$18 already authorized).

If the fleet can't do that, fine-tune + serve are fiction built on a model it can't operate.

## Status
Awaiting khalid ok/no on: collapse to the ONE MVP + capped Stage-1 burst. Kill fine-tune/serve until the data floor is real.
