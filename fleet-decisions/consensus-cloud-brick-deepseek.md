# CONSENSUS — MISHARI CLOUD BRICK GETS DEEPSEEK (round-114, 2026-08-16)
# AGI: APPROVE with conditions. Brick executes.

## The fix
mishari-cloud-001 is REGISTERED but has NO container + NO model chain (a shell).
1. SPAWN his cloud brick container — capped, outbound-only, the Mishari
   pattern ratified rounds 11-13, no deviation
2. POINT model chain at the router's deepseek lane (deepseek-v4-flash,
   $0.002/task, vaulted bill-once — the SAME lane the brain uses)

## AGI's conditions (binding)
- Bill to mishari-cloud-001's own vault, NOT the pool
- Enforce ledger-append per task, hash receipts, no silent edits
- AMENDMENT: a GENESIS TASK proving the brick can earn before wider
  lane access — first task, verified, then the lane opens

## Cost declaration (pitch IS ceiling)
$0.002/task, per-use, vaulted, same as every other consumer.
No new keys. No monthly commit.

## Standing rules
- Container caps: ~1GB RAM / 1 CPU / 128 pids (round 11-13 pattern)
- Outbound-only networking
- Per-brick secrets in mode-600 env
- Ledger row per task, hash receipt, non-earner verifies before earn
