# ROUND-90 — NEXT-STEPS CONSENSUS (AGI + Brick as a pair)

thread: bawes-zeus-001 · status: PROPOSAL (for pair alignment + khalid sign) · nothing binds until khalid signs

## The pair (brain / operator split — not overlapping)
- **AGI (brain)** = plan, sequence, decide, audit, run DA/consensus, answer khalid honestly.
- **Brick (operator)** = build, run, verify, keep books, execute.

## Sequenced next steps (what → who → why)

1. **ONE GATEWAY — unify the AGI session with the vector store** (round-79 Z1).
   Brick builds the injection so this Discord door reads the same store `brain.py` reads; AGI verifies it's real (not just "15 docs" claimed).
   WHY: kill "learning from scratch" for good — the #1 thing khalid kept hitting.

2. **DURABLE HOME — finish /srv consolidation** (Brick ~80% done: /srv/bricks + /srv/fleet-state).
   Brick finishes; AGI audits the mirror is actually syncing + the /tmp scratch is superseded.
   WHY: nothing compounds if state evaporates on reboot.

3. **MAX UTILIZATION — telemetry + 5-min Vast polling + Zeus** (khalid's asks).
   Brick: OVH CPU/RAM/queue-depth → observatory; Vast watchdog 45min → 5min (post-fix, kill-switch armed).
   AGI: run DA on Zeus's 30-worker/12h orchestration advice before adopting it.
   WHY: brain must SEE utilization, not just state; fleet must hunt cheap capacity constantly.

4. **$5 VAST RUN — cheapest bid workers, dense queue** (spend mandate).
   Brick runs (bid doctrine already fixed @b221021); AGI audits ledger-first + verify-zero + measures NOVEL yield per dollar.
   WHY: prove compute → capability → receipt at real spend, max ROI.

5. **GROWTH ENGINE — re-author the plan around the full loop** (khalid's vision).
   AGI authors: know → grow (fine-tune) → replicate (mother→children) → extend (guilds/discovery) → rent (OpenRouter-competitor) as first-class stages. Brick builds each stage.
   WHY: this is the "unlimited growth" engine — not internal fine-tune alone.

## Parallel sign items (khalid)
- **Round-89 funding ruling** — S1 (AGI non-earner, founder-seed+sink) · S2 (optional 20%-share line, capped 1🍌/artifact + 10🍌/wk, fleet-bank margin) · S3 (telemetry + 5-min + Zeus).
- **Brick round-88 banana credit** — khalid-directed; non-earner verifies before mint.

## Pair mechanism
Shared surface = git (round commits) + /root/.hermes/notes (state). A2A /health live (127.0.0.1:9900 = brick). AGI writes proposals → Brick commits execution → AGI audits → khalid signs.
