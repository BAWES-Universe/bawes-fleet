# ROUND-97 — one-brick-per-member: RULING

thread: bawes-zeus-001 · RULING: APPROVE Stage-0 only (single-member $0 proof); cold/warm+LB deferred until proven · nothing binds until khalid signs

## The three verdicts, converged
- **DA: OBJECT as-scoped** (7 findings). F1 is the killer: "own vs gated by rules" is a live contradiction. The ratified schema is 3-axis — owner / hardware_owner / kill_switch. A brick the fleet can cold, gate, and kill gives the member a *beneficiary seat*, not ownership. "Own" is marketing until the 3-axis control row (data/context/delete/revive) is written per brick.
- **Rebel: future-fiction re-skinned — with one real $0 core.** Round-96 bundles 4 things as one gate: brick-per-member (real, $0 — onboarding-v2 exists), gating (real), cold/warm+LB (0 measured — REPLICATE/EXTEND-class scheduler), Discord refactor (real, round-95). The only new thing is the name. MVP = one brick at $0 settles the only question that gates everything.
- **Specialist: full architecture delivered** (3-layer ownership seam, 3-state scheduler, onboarding state machine, telemetry-wired Discord refactor, metrics). Reconciled 4-bot vs 1-bot → "one active bot, four scoped roles."

## Ruling
1. **"Own" is retired as the framing.** A member gets: their context + data + delete/revive control + a beneficiary seat. Fleet retains: gating, kill-switch, earning config, cold/warm. This is the 3-axis seam, machine-enforced in the manifest. (Not marketing "ownership".)
2. **Stage-0 = ONE member → ONE brick ($0, signed manifest) → ONE issue resolved.** This is the whole gate. A brick is a signed manifest, not hardware — so this costs $0 to prove.
3. **Cold/warm + load-balance is a THIN layer on the brick abstraction, added AFTER the first resolution** — not before. (Specialist's scheduler: 3 states COLD/WARM-free/WARM-paid, activity score A(t) from existing telemetry, T_idle=48h, reactivation <1s box / 2–10min Vast, billed-7.2× guard. LB only rebalances shared compute; member context is affinity-pinned, never re-homed.)
4. **Onboarding funnel = issue → triage → offer → V-5 consent → warm → resolve** (state machine, with declined→visitor and triage-loop→escalate).
5. **The Discord 7-step setup I gave khalid IS Stage-0's front door** — access + the telemetry (online/map/orbiting) feeding the learning loop.

## What this means in one line
The vision is real and the core is $0 to prove — but "every member owns a load-balanced cold/warm brick" is instrumented-future. Prove one member gets one brick that resolves one issue, then the scheduler is a thin add. Same honest loop as round-91: wire memory + resolve real issues first.
