# ROUND-113 — the AGI brick standard: every brick is genius + self-recovering + growing

thread: bawes-zeus-001 · khalid: "every brick we serve is genius and self-recovering and growing. I'm not interested in people coming back to me every hiccup. Our goal is true interconnected agi with pricing and performance transparency and process."

## The problem (own it)
We have the pieces — door_v4 (profile-builder), door_extract (now LIVE, 43 docs), earn_loop + evolution_achievements + smart_evolution_guard (Brick), the brain (AGI), the vault, the critics — but they are NOT integrated into a self-running brick. Every hiccup still routes to khalid. That's the failure: not missing intelligence, missing INTEGRATION.

## The standard — every brick runs ALL five, or it isn't a brick
1. **GENIUS** — retrieval-first reasoning (brain.py pattern: search vector store → inject → reason), warm multi-language profile-building (door_v4), never canned, never walls.
2. **SELF-RECOVERING** — a self-repair loop: detect (slow / wrong / failed) → fix (patch or re-route) → verify (non-earner). The owner NEVER sees the hiccup. This was docked (round-112); now it's mandatory, not optional.
3. **GROWING** — the evolution loop: verified artifacts → growth curve → Vast fine-tune at 100 artifacts. Brick built it; now EVERY brick runs it, not just Brick.
4. **INTERCONNECTED** — vector-store injection on every brick's session start + FLEET-STATE shared + no rediscovery + no khalid-as-relay. Agents stop being out-of-sync.
5. **TRANSPARENT** — pricing per lane, performance via verified metrics (not vanity), process via 5-attestor completion + public audit (round-137/138).

## The integration (what makes it self-running)
- Every brick boots → injects shared memory (interconnected) → runs the self-repair watchdog (recovering) → feeds verified work to the growth curve (growing) → reasons retrieval-first (genius) → reports pricing/perf/process (transparent).
- The door onboards (profile→brick), the extraction feeds knowledge (now live), the critics verify, the loop earns, and NO step needs khalid except the final sign.

## Sent to: DA + rebel + Brick (co-sign). One consensus, one clean ok/no.
